{
  "name": "dataverse-component-inspector",
  "displayName": "Dataverse Component Inspector",
  "description": "Inline annotations for Dataverse GUIDs inside solution XML files.",
  "version": "0.0.1",
  "publisher": "inlineexplorer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://example.com/Inline-Explorer-designed-for-Dataverse.git"
  },
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:dataverse.login",
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dataverse.login",
        "title": "Dataverse: Login"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "lint": "eslint .",
    "prepare": "npm run compile",
    "test": "npm run test:unit",
    "test:build": "tsc -p tsconfig.tests.json",
    "test:unit": "npm run test:build && mocha --require build/tests/tests/setup.js \"build/tests/tests/unit/**/*.spec.js\"",
    "test:integration": "npm run test:build && mocha --require build/tests/tests/setup.js \"build/tests/tests/integration/**/*.spec.js\"",
    "test:coverage": "npm run test:build && c8 -o coverage --reporter=text --reporter=lcov mocha --require build/tests/tests/setup.js \"build/tests/tests/unit/**/*.spec.js\"",
    "test:ci": "npm run lint && npm run test:coverage && npm run test:integration"
  },
  "devDependencies": {
    "@types/chai": "^4.3.12",
    "@types/mocha": "^10.0.10",
    "@types/mock-require": "^3.0.0",
    "@types/node": "^18.19.3",
    "@types/node-fetch": "^2.6.13",
    "@types/sinon": "^17.0.4",
    "@types/vscode": "^1.84.0",
    "@typescript-eslint/eslint-plugin": "^6.10.0",
    "@typescript-eslint/parser": "^6.10.0",
    "c8": "^10.1.3",
    "chai": "^4.3.10",
    "eslint": "^8.57.0",
    "mocha": "^11.7.4",
    "mock-require": "^3.0.3",
    "sinon": "^21.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@azure/identity": "^4.4.0",
    "@azure/msal-node": "^2.6.4",
    "node-fetch": "^2.6.9"
  }
}
